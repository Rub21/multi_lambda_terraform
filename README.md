# Multi AWS Lambda creation using Terrraform

Simple Terrraform tamples to deploy scrpit the ins in the `scripts` folder.

```sh
    terraform init
    terraform plan
    terraform apply -auto-approve
    terraform delete -auto-approve
```